{
  "name": "vk-x",
  "version": "0.0.0",
  "scripts": {
    "test": "lerna run test",
    "postinstall": "lerna bootstrap",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^2.5.1"
  }
}
